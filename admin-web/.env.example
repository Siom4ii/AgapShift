# Copy to .env and fill with the same project URL + anon key as the mobile app
# (Dashboard → Settings → API). Restart `npm run dev` after changing.

VITE_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=your_anon_key
